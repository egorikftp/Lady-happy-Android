package com.egoriku.mainscreen.di.module

import dagger.Module

@Module
class MainActivityModule {

   /* @Provides
    @IntoMap
    @ActivityScope
    @ViewModelKey(MainActivityViewModel::class)
    fun provideMaindActivityViewModel(router: IRouter, analytics: IAnalytics): ViewModel = MainActivityViewModel(router, analytics)*/
}